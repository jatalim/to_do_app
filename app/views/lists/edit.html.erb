<%= form_for @list, url: list_path(id: @list.id), method: :put do |f| %>
 	<div class = "row">
 	<%= f.label :list_name %>
 	</div>

 	<div class = "row">
  	<%= f.text_field :list_name %>
	</div>

	<div class = "row">
  	<%= f.submit "Update" %>
	</div>

<% end %>
